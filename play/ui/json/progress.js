{
    setup() {
        const value1 = Vue.ref(30)
        const value2 = Vue.ref(40)
        const value3 = Vue.ref(50)
        const value4 = Vue.ref(60)

        return { value1, value2, value3, value4 }
    }
}
